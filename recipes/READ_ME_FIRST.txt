UNHANGRY — SNACKS + AI ADVICE UPDATE

WHAT'S HERE:
- snack-files/  : 40 snack .json files, already in your recipe schema (mealType "snack")
- PASTE_INTO_CLAUDE_CODE.md : the single prompt to run

HOW TO USE:
1. Copy all 40 files from snack-files/ into the SAME folder as your recipes
   (the one with kafta-sandwiches.json etc). They use your exact schema, so
   the app reads them automatically as Snacks & Sides.
2. Open Claude Code, paste the contents of PASTE_INTO_CLAUDE_CODE.md.
3. Let it show you the AI Advice output with real data before polishing visuals.

The only new field is "primaryMacro" — used by AI Advice to match a snack to a
person's macro gap. Everything else matches your existing recipe format.
